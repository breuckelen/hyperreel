# @package _group_

type: geometry_over
ray_chunk: 32768
batch_size: 8192

fields: ['raw_points', 'raw_distance']

contract:
  type: mipnerf
  contract_samples: True
  use_dataset_bounds: True

weight:
  type: exponential_decay
  start: 0.25
  decay: 0.1
  num_epochs: 50


